def calc(x):
    perimeter = 4 * side
    area = side ** 2
    return perimeter, area
    
side = int(input())
perimeter, area = calc(side)

print("Perimeter: " + str(perimeter))
print("Area: " + str(area))
